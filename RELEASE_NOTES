A new version of Utils is out. It is for PHP 8.3 or later.

null is no longer accepted as index in (T)Collection (it is deprecated as index for arrays in PHP 8.5).

For complete list of changes since previous version, see CHANGELOG.md.
